import styled from "styled-components";
import {
  NavigationLink,
  Title,
  Image,
  Text,
  OuterLinkWrapper,
  OuterLink,
} from "components/ArticleContent/styles";

const HomeLink = styled(NavigationLink)``;

const NewsTitle = styled(Title)``;

const NewsImage = styled(Image)``;

const NewsText = styled(Text)``;

const LinkWrapper = styled(OuterLinkWrapper)``;

const StyledLink = styled(OuterLink)``;

export { HomeLink, NewsTitle, NewsImage, NewsText, LinkWrapper, StyledLink };
